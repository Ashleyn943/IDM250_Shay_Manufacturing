document.addEventListener(‘DOMContentLoaded’, initialize);

    function initialize() {
        fetch('URL.com')
        .then(res => res.json())
        .then(data => (
            const container = document.getElementByID('box')
        ))
            if (data.sucess && container)

        };
